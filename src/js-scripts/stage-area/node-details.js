function doNodeDetails() {
    if (!dfm.modelEditMode) return;

    let editMode="new";
    displayNodeDetailsModal(editMode);
}

function displayNodeDetailsModal(editMode) {

    if (editMode = "new") {
        // Clear the modal form fields
    }

}